services:
  docs:
    image: carda_link_local_docs
    container_name: carda_link_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./carda_link:/app/carda_link:z
    ports:
      - '9000:9000'
    command: /start-docs
